version: "3.4"
services:
  api:
    build:
      context: .
      target: release
    command: "--reload"
    container_name: fastapi
    networks:
      - "default"
    ports:
      - 8000:8000
    volumes:
      - ./src:/app
